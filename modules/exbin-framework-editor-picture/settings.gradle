rootProject.name = 'exbin-framework-editor-picture'

if (rootProject.path == ':') {
    include ':modules:modules:exbin-framework-api'
}

apply from: '../exbin-framework-java-modules-settings.gradle'
