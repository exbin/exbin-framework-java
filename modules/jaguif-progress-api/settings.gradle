rootProject.name = 'jaguif-progress-api'

if (rootProject.path == ':') {
    include ':core:jaguif-core'
    include ':modules:jaguif-operation-api'
    include ':modules:jaguif-utils'
}

apply from: '../jaguif-modules-settings.gradle'
