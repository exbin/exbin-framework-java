group = 'org.exbin.framework'
ext.libraryName = 'ExBin Framework Link Support Library'
ext.libraryDescription = 'Library for link support in ExBin Framework'

apply from: '../modules.gradle'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
    flatDir {
        dirs "../../lib"
    }
}

dependencies {
    compile(project(':modules:exbin-framework'))
    compile(project(':modules:exbin-framework-gui-link-api'))
    compile(project(':modules:exbin-framework-gui-frame-api'))
    compile(project(':modules:exbin-framework-gui-utils'))
    testCompile group : 'junit', name: 'junit', version: '4.10'
}
